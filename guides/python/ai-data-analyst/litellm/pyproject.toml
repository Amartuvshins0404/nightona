[project]
name = "ai-data-analyst-litellm"
version = "0.1.0"
description = "Nightona + LiteLLM data analyst example in Python"
requires-python = ">=3.10,<4.0"

dependencies = [
  "litellm",
  "nightona",
]

[tool.setuptools]
py-modules = []
