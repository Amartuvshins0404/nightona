#!/bin/bash
set -e

# Reload systemd daemon to remove the service
systemctl daemon-reload

# On purge, remove configuration and data
if [ "$1" = "purge" ]; then
    rm -rf /etc/nightona/runner.env
    rm -rf /var/lib/nightona/runner
    rm -rf /var/log/nightona
fi

exit 0
