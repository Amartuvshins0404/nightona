---
title: "Errors"
hideTitleOnPage: true
---

## NightonaException

Base exception for all Nightona SDK errors.

Subclasses map to specific HTTP status codes and allow callers to catch
precise failure conditions without string-parsing error messages:
```java
try {
Sandbox sandbox = nightona.sandbox().get("nonexistent-id");
} catch (NightonaNotFoundException e) {
// sandbox does not exist
} catch (NightonaAuthenticationException e) {
// invalid API key
} catch (NightonaException e) {
// other SDK error
}
```

### Constructors

#### new NightonaException()
```java
public NightonaException(String message)
```

Creates a generic Nightona exception.

**Parameters**:

- `message` _String_ - error description

#### new NightonaException()
```java
public NightonaException(String message, Throwable cause)
```

Creates a generic Nightona exception with a cause.

**Parameters**:

- `message` _String_ - error description
- `cause` _Throwable_ - root cause

#### new NightonaException()
```java
public NightonaException(int statusCode, String message)
```

Creates a Nightona exception with explicit HTTP status code.

**Parameters**:

- `statusCode` _int_ - HTTP status code
- `message` _String_ - error description

#### new NightonaException()
```java
public NightonaException(int statusCode, String message, Throwable cause)
```

Creates a Nightona exception with explicit HTTP status code and a cause.

**Parameters**:

- `statusCode` _int_ - HTTP status code
- `message` _String_ - error description
- `cause` _Throwable_ - root cause

#### new NightonaException()
```java
public NightonaException(int statusCode, String message, Map<String, String> headers)
```

Creates a Nightona exception with HTTP status code and headers.

**Parameters**:

- `statusCode` _int_ - HTTP status code
- `message` _String_ - error description
- `headers` _Map\<String, String\>_ - response headers

### Methods

#### getStatusCode()
```java
public int getStatusCode()
```

Returns the HTTP status code, or 0 if not applicable.

**Returns**:

- `int` -

#### getHeaders()
```java
public Map<String, String> getHeaders()
```

Returns the HTTP response headers, or an empty map if not available.

**Returns**:

- `Map\<String, String\>` -

## NightonaAuthenticationException

Raised when API credentials are missing or invalid (HTTP 401).
```java
try {
nightona.sandbox().create();
} catch (NightonaAuthenticationException e) {
System.err.println("Invalid or missing API key");
}
```

### Constructors

#### new NightonaAuthenticationException()
```java
public NightonaAuthenticationException(String message)
```

Creates an authentication exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NightonaAuthenticationException()
```java
public NightonaAuthenticationException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NightonaBadRequestException

Raised when the request is malformed or contains invalid parameters (HTTP 400).
```java
try {
nightona.sandbox().create(params);
} catch (NightonaBadRequestException e) {
System.err.println("Invalid request parameters: " + e.getMessage());
}
```

### Constructors

#### new NightonaBadRequestException()
```java
public NightonaBadRequestException(String message)
```

Creates a bad-request exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NightonaBadRequestException()
```java
public NightonaBadRequestException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NightonaConflictException

Raised when an operation conflicts with the current state (HTTP 409).

Common causes: creating a resource with a name that already exists,
or performing an operation incompatible with the resource's current state.
```java
try {
nightona.snapshot().create(params);
} catch (NightonaConflictException e) {
System.err.println("A snapshot with this name already exists");
}
```

### Constructors

#### new NightonaConflictException()
```java
public NightonaConflictException(String message)
```

Creates a conflict exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NightonaConflictException()
```java
public NightonaConflictException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NightonaConnectionException

Raised for network-level connection failures (no HTTP response received).

Raised when the SDK cannot reach the Nightona API due to network issues
such as DNS failure, connection refused, or TLS errors.
```java
try {
nightona.sandbox().create();
} catch (NightonaConnectionException e) {
System.err.println("Cannot reach Nightona API: " + e.getMessage());
}
```

### Constructors

#### new NightonaConnectionException()
```java
public NightonaConnectionException(String message)
```

Creates a connection exception.

**Parameters**:

- `message` _String_ - connection failure description

#### new NightonaConnectionException()
```java
public NightonaConnectionException(String message, Throwable cause)
```

Creates a connection exception with a cause.

**Parameters**:

- `message` _String_ - connection failure description
- `cause` _Throwable_ - root cause

## NightonaForbiddenException

Raised when the authenticated user lacks permission to perform an operation (HTTP 403).
```java
try {
nightona.sandbox().delete(sandboxId);
} catch (NightonaForbiddenException e) {
System.err.println("Not authorized to delete this sandbox");
}
```

### Constructors

#### new NightonaForbiddenException()
```java
public NightonaForbiddenException(String message)
```

Creates a forbidden exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NightonaForbiddenException()
```java
public NightonaForbiddenException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NightonaNotFoundException

Raised when a requested resource does not exist (HTTP 404).

### Constructors

#### new NightonaNotFoundException()
```java
public NightonaNotFoundException(String message)
```

Creates a not-found exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NightonaNotFoundException()
```java
public NightonaNotFoundException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NightonaRateLimitException

Raised when API rate limits are exceeded (HTTP 429).

### Constructors

#### new NightonaRateLimitException()
```java
public NightonaRateLimitException(String message)
```

Creates a rate-limit exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NightonaRateLimitException()
```java
public NightonaRateLimitException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NightonaServerException

Raised for unexpected server-side failures (HTTP 5xx).

These are typically transient and safe to retry with exponential backoff.
```java
try {
nightona.sandbox().create();
} catch (NightonaServerException e) {
System.err.println("Server error (status " + e.getStatusCode() + "), retry later");
}
```

### Constructors

#### new NightonaServerException()
```java
public NightonaServerException(int statusCode, String message)
```

Creates a server exception.

**Parameters**:

- `statusCode` _int_ - HTTP status code (typically 5xx)
- `message` _String_ - error description from the API

#### new NightonaServerException()
```java
public NightonaServerException(int statusCode, String message, Throwable cause)
```

**Parameters**:

- `statusCode` _int_ - HTTP status code (typically 5xx)
- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause

## NightonaTimeoutException

Raised when an SDK operation times out.

This exception is generated client-side and is not tied to a single HTTP status code.

### Constructors

#### new NightonaTimeoutException()
```java
public NightonaTimeoutException(String message, Throwable cause)
```

Creates a timeout exception with a cause.

**Parameters**:

- `message` _String_ - timeout description
- `cause` _Throwable_ - root cause

#### new NightonaTimeoutException()
```java
public NightonaTimeoutException(String message)
```

Creates a timeout exception.

**Parameters**:

- `message` _String_ - timeout description

## NightonaValidationException

Raised for semantic validation failures (HTTP 422).

Raised when the request is well-formed but the values fail business logic
validation (e.g., unsupported resource class, invalid configuration).
```java
try {
nightona.sandbox().create(params);
} catch (NightonaValidationException e) {
System.err.println("Validation failed: " + e.getMessage());
}
```

### Constructors

#### new NightonaValidationException()
```java
public NightonaValidationException(String message)
```

Creates a validation exception.

**Parameters**:

- `message` _String_ - error description from the API

#### new NightonaValidationException()
```java
public NightonaValidationException(String message, Throwable cause)
```

**Parameters**:

- `message` _String_ - error description from the API
- `cause` _Throwable_ - root cause
