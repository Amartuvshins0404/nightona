---
title: "Nightona"
hideTitleOnPage: true
---

## Nightona

Nightona class for Nightona SDK.

### Constructors

#### new Nightona()

```ruby
def initialize(config)

```

**Parameters**:

- `config` _Nightona:Config_ - Configuration options. Defaults to Nightona::Config.new

**Returns**:

- `Nightona` - a new instance of Nightona

### Methods

#### config()

```ruby
def config()

```

**Returns**:

- `Nightona:Config`

#### api_client()

```ruby
def api_client()

```

**Returns**:

- `NightonaApiClient`

#### sandbox_api()

```ruby
def sandbox_api()

```

**Returns**:

- `NightonaApiClient:SandboxApi`

#### volume()

```ruby
def volume()

```

**Returns**:

- `Nightona:VolumeService`

#### object_storage_api()

```ruby
def object_storage_api()

```

**Returns**:

- `NightonaApiClient:ObjectStorageApi`

#### snapshots_api()

```ruby
def snapshots_api()

```

**Returns**:

- `NightonaApiClient:SnapshotsApi`

#### snapshot()

```ruby
def snapshot()

```

**Returns**:

- `Nightona:SnapshotService`

#### close()

```ruby
def close()

```

Shuts down OTel providers, flushing any pending telemetry data.

**Returns**:

- `void`

#### create()

```ruby
def create(params, on_snapshot_create_logs:)

```

Creates a sandbox with the specified parameters

**Parameters**:

- `params` _Nightona:CreateSandboxFromSnapshotParams, Nightona:CreateSandboxFromImageParams, Nil_ - Sandbox creation parameters

**Returns**:

- `Nightona:Sandbox` - The created sandbox

**Raises**:

- `Nightona:Sdk:Error` - If auto_stop_interval or auto_archive_interval is negative

#### delete()

```ruby
def delete(sandbox)

```

Deletes a Sandbox.

**Parameters**:

- `sandbox` _Nightona:Sandbox_ -

**Returns**:

- `void`

#### get()

```ruby
def get(id)

```

Gets a Sandbox by its ID.

**Parameters**:

- `id` _String_ -

**Returns**:

- `Nightona:Sandbox`

#### list()

```ruby
def list(query)

```

Iterates over Sandboxes matching the given query.

**Parameters**:

- `query` _Nightona:ListSandboxesQuery, nil_ - Optional filters, sorting, and per-page size.

**Returns**:

- `Enumerator\<Nightona:Sandbox\>`

**Raises**:

- `Nightona:Sdk:Error` -

**Examples:**

```ruby
nightona.list(Nightona::ListSandboxesQuery.new(labels: { 'env' => 'dev' })).each do |sandbox|
  puts sandbox.id
end

```

#### start()

```ruby
def start(sandbox, timeout)

```

Starts a Sandbox and waits for it to be ready.

**Parameters**:

- `sandbox` _Nightona:Sandbox_ -
- `timeout` _Numeric_ - Maximum wait time in seconds (defaults to 60 s).

**Returns**:

- `void`

#### stop()

```ruby
def stop(sandbox, timeout)

```

Stops a Sandbox and waits for it to be stopped.

**Parameters**:

- `sandbox` _Nightona:Sandbox_ -
- `timeout` _Numeric_ - Maximum wait time in seconds (defaults to 60 s).

**Returns**:

- `void`
